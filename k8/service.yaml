apiVersion: v1
kind: Service
metadata:
  name: aks-sample
  namespace: demo
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-internal: "true"
spec:
  type: LoadBalancer
  loadBalancerIP: 10.20.2.100
  selector:
    app: aks-sample
  ports:
    - port: 80
      targetPort: 80